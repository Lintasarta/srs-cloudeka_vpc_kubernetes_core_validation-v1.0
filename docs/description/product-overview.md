# Product Overview

Cloudeka VPC (Virtual Private Cloud) is a tool for cloud users to limit or permit access for each application deployed inside the Deka GPU Kubernetes environment. It is deployed in Core Validation as a Custom Resource Definition (CRD) named **CloudekaVPC**.
